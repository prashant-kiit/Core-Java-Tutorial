package com.Prashant.Access;

class Child2 {
    private int num = 480;
    // final(value fixed) + static(address fixed)= const(both fixed)
    // global and local are relative terms
    protected int getNum() {
        return num;
    }
}
